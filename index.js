/**
 * for each task(feature branch) you create a new branch from the main branch and then merge it back into the main branch when the task is complete.
 * This way, you can work on multiple features simultaneously without interfering with each other's work.
 * **/
/**
 * @param {HTMLElement} writeButton - The button element that triggers the write operation.
 * @param {HTMLElement} readButton - The button element that triggers the read operation.
 * @param {HTMLElement} deleteButton - The button element that triggers the delete operation.
 *
 */
